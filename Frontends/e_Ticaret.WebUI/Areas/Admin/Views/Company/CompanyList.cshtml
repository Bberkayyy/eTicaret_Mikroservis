@model IEnumerable<ResultCompanyDto>
@{
    ViewData["Title"] = "CompanyList";
    Layout = "~/Areas/Admin/Views/AdminLayout/_AdminLayout.cshtml";
    int count = 0;
}
<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            @if (Model.Count() > 0)
            {
                <div class="chart-box">
                    <p>Kargo şirketleri ile ilgili işlemlerinizi aşağıdaki tablodan gerçekleştirebilirsiniz.</p>
                    <hr />
                    <div class="bs-example" data-example-id="simple-responsive-table">
                        <div class="table-responsive m-top-2">
                            <table class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Şirket Adı</th>
                                        <th>Sil</th>
                                        <th>Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.Name</td>
                                            <td><a href="/admin/cargocompany/deletecompany/@item.Id" class="btn btn-danger">Sil</a></td>
                                            <td><a href="/admin/cargocompany/updatecompany/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/admin/cargocompany/createcompany/" class="btn btn-primary">Kargo Şirketi Ekle</a>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <p class="bg-danger text-white p-4 text-center table-responsive">Kargo şirketi bulunamadı. Kargo şirketi eklemek için "Kargo Şirketi Ekle" butonuna tıklayınız.</p>
                <a href="/admin/cargocompany/createcompany/" class="btn btn-primary">Kargo Şirketi Ekle</a>
            }
        </div>
    </div>
</section>