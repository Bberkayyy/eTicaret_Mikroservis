@model IEnumerable<ResultFeatureSliderDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/_AdminLayout.cshtml";
    int count = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            @if (Model.Count() > 0)
            {
                <div class="chart-box">
                    <p>Öne çıkan görseller ile ilgili işlemlerinizi aşağıdaki tablodan gerçekleştirebilirsiniz.</p>
                    <hr />
                    <div class="bs-example" data-example-id="simple-responsive-table">
                        <div class="table-responsive m-top-2">
                            <table class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Başlık</th>
                                        <th>Açıklama</th>
                                        <th>Görsel</th>
                                        <th>Durumu</th>
                                        <th>Sil</th>
                                        <th>Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Title</td>
                                            <td>@item.Description</td>
                                            <td>@item.ImageUrl</td>
                                            <td>@(item.Status ? "Aktif" : "Pasif")</td>
                                            <td><a href="/admin/featureslider/deletefeatureslider/@item.Id" class="btn btn-danger">Sil</a></td>
                                            <td><a href="/admin/featureslider/updatefeatureslider/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/admin/featureslider/createfeatureslider/" class="btn btn-primary">Öne Çıkan Görsel Ekle Ekle</a>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <p class="bg-danger text-white p-4 text-center table-responsive">Öne çıkan görsel bilgisi bulunamadı. Öne çıkan görsel eklemek için "Öne Çıkan Görsel Ekle" butonuna tıklayınız.</p>
                <a href="/admin/featureslider/createfeatureslider/" class="btn btn-primary">Öne Çıkan Görsel Ekle Ekle</a>
            }
        </div>
    </div>
</section>

