@model IEnumerable<ResultDiscountOfferDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/_AdminLayout.cshtml";
    int count = 0;
}
<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            @if (Model.Count() > 0)
            {
                <div class="chart-box">
                    <p>İndirim teklifleri ile ilgili işlemlerinizi aşağıdaki tablodan gerçekleştirebilirsiniz. Sayfa bütünlüğü için en fazla <strong>2 adet</strong> indirim teklifi aktif olmalıdır.</p>
                    <hr />
                    <div class="bs-example" data-example-id="simple-responsive-table">
                        <div class="table-responsive m-top-2">
                            <table class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Başlık</th>
                                        <th>İndirim Oranı</th>
                                        <th>Görsel</th>
                                        <th>Durum</th>
                                        <th>Sil</th>
                                        <th>Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Title</td>
                                            <td>@item.DiscountRate</td>
                                            <td>@item.ImageUrl</td>
                                            <td>@(item.IsActive ? "Aktif" : "Pasif")</td>
                                            <td><a href="/admin/discountoffer/deletediscountoffer/@item.Id" class="btn btn-danger">Sil</a></td>
                                            <td><a href="/admin/discountoffer/updatediscountoffer/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/admin/discountoffer/creatediscountoffer/" class="btn btn-primary">İndirim Teklifi Ekle</a>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <p class="bg-danger text-white p-4 text-center table-responsive">İndirim teklifi bilgisi bulunamadı. İndirim teklifi eklemek için "İndirim Teklifi Ekle" butonuna tıklayınız.</p>
                <a href="/admin/discountoffer/creatediscountoffer/" class="btn btn-primary">İndirim Teklifi Ekle</a>
            }
        </div>
    </div>
</section>